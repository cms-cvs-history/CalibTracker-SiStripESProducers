process Test = {
    
    source = EmptyIOVSource {
        string timetype = "runnumber"
        untracked uint32 firstRun = 1
	untracked uint32 lastRun = 1
        uint32 interval = 1
    }
    
    service = MessageLogger {
	untracked vstring destinations = { "cout" }
	untracked PSet cout = { untracked string threshold = "DEBUG" }
	untracked vstring debugModules   = { "*" }
    }

    service = Timing { }
    
    es_source = SiStripFedCablingFakeESSource { 
	FileInPath DetIdsFile = "CalibTracker/SiStripCommon/data/SiStripDetInfo.dat"
	FileInPath FedIdsFile = "CalibTracker/SiStripCommon/data/SiStripFedIdListReader.dat"
    }
    
    module test = test_AnalyzeCabling { }

    module print = AsciiOutputModule { }

    path p = { test }
    
    endpath e = { print }

}

